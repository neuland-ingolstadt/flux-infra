apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: outline-storage-pvc-claim
  namespace: verein-int
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 1Gi
