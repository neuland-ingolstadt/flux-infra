apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: aoc-leaderboard
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: aoc-leaderboard
  policy:
    numerical:
      order: asc
  filterTags:
    pattern: '^build-([0-9]+)$'
    extract: '$1'
