# Strategic merge patch: ensure strategy has only type: Recreate (no rollingUpdate).
# Required because Recreate forbids rollingUpdate; existing cluster object may still have it.
apiVersion: apps/v1
kind: Deployment
metadata:
  name: vaultwarden
  namespace: passwords
spec:
  strategy:
    $retainKeys: ["type"]
    type: Recreate
