apiVersion: v1
kind: Service
metadata:
  name: aptabase
  namespace: monitoring
spec:
  type: ClusterIP
  selector:
    app: aptabase
  ports:
    - name: http
      port: 8080
      protocol: TCP
      targetPort: http
