apiVersion: v1
kind: Service
metadata:
  name: aptabase-db
  namespace: monitoring
spec:
  type: ClusterIP
  selector:
    app: aptabase-db
  ports:
    - name: postgres
      port: 5432
      protocol: TCP
      targetPort: postgres
