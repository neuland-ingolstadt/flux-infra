apiVersion: v1
kind: Service
metadata:
  name: pretix
  namespace: verein-ext
spec:
  type: ClusterIP
  selector:
    app: pretix
  ports:
    - name: http
      port: 80
      protocol: TCP
      targetPort: http
