apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: cl-events-frontend
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: cl-events-frontend
  policy:
    numerical:
      order: asc
  filterTags:
    pattern: '^build-([0-9]+)$'
    extract: '$1'