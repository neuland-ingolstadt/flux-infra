apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: cl-events-postgres-pvc
  namespace: verein-ext
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 500Mi
