---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: neuland-next-staging
  namespace: neuland-app
  annotations:
    traefik.ingress.kubernetes.io/router.tls: "true"
spec:
  ingressClassName: traefik
  tls:
  - hosts:
    - dev.neuland.app
    secretName: neuland-app-tls
  rules:
  - host: dev.neuland.app
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: neuland-next-staging
            port:
              number: 3000
