apiVersion: image.toolkit.fluxcd.io/v1
kind: ImagePolicy
metadata:
  name: neuland-dashboard
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: neuland-dashboard
  policy:
    numerical:
      order: asc
  filterTags:
    pattern: '^main-([0-9]+)$'
    extract: '$1'
