apiVersion: v1
kind: Service
metadata:
  name: neuland-api-staging
  namespace: neuland-app
spec:
  type: ClusterIP
  selector:
    app: neuland-api-staging
  ports:
    - name: http
      port: 4000
      protocol: TCP
      targetPort: http
