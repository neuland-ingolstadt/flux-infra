apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: neuland-next-docs
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: neuland-next-docs
  policy:
    numerical:
      order: asc
  filterTags:
    pattern: '^build-([0-9]+)$'
    extract: '$1'
