---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ersti-kit-staging
  namespace: neuland-app
  annotations:
    traefik.ingress.kubernetes.io/router.tls: "true"
    cert-manager.io/cluster-issuer: "letsencrypt"
spec:
  ingressClassName: traefik
  tls:
  - hosts:
    - dev.ersti.eggl.one
    secretName: dev-ersti-eggl-one-tls
  rules:
  - host: dev.ersti.eggl.one
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: ersti-kit-staging
            port:
              number: 3000
